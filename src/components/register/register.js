import React, { Component } from 'react';
import './register.css';

/**
 * Registers are temporary holding spaces for cards
 * outside of the arrays.
 */

export default class Register extends Component {
    render(){
        return (
            <div className="register">
                A register
            </div>
        );
    }
}